<div class="btn btn-custom" id="lucky-button">
  <p>
    Find me a lunchmate!
  </p>
</div>

<div class="gif-container d-none"  id="lucky-loading-image">
  <%= image_tag "Lunchmate-picking-2.gif", class: "loading-image" %>
</div>

<div class="container d-none" id="lucky-page">
  <div class="row">
<div class="position-relative back-btn">
<%= link_to '', :back , class: "absolute-0-0"%>
<%= '<i class="fas fa-chevron-left back-btn"></i>'.html_safe %>
</div>
    <div class="col-12 my-4">
      <h1> You guys should sit together!</h1>
        <% if @user.photo.attached? %>
          <%= link_to user_path(@user) do %>
            <%= cl_image_tag @user.photo.key, width: 150, height: 150, crop: :fill, class: "avatar2 left-avatar"%>
          <% end %>
        <% end %>
        
        <% if @current_user.photo.attached? %>
          <%= link_to user_path(@current_user) do %>
            <%= cl_image_tag @current_user.photo.key, width: 150, height: 150, crop: :fill, class: "avatar2 right-avatar"%>
          <% end %>
        <% end %>
     <div>
     <div class="match-msg">
      <br>
        <p><strong><%= @user.first_name %></strong> is also goinig to Little Green Rabbit for lunch!
        Both of you love <strong><i><%= current_user.preference.downcase %></i></strong> cuisine!
        You guys should totally sit together. </p>
        <p>Click “Yes” to join the lunch!</p>
     </div>
      <%= link_to "Yes!", event_path(@event.id), class: "btn btn-custom", id: "yes-button"%>
     </div>
    </div>
  </div>
</div>
